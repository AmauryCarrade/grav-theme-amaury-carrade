enabled: true

# Required for the theme blueprints to work. (Undocumented??)
streams:
  schemes:
    blueprints:
      type: ReadOnlyStream
      paths: ['themes://nebulius/blueprints']

header:
  title: ''
  tagline: ''
